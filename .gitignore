node_modules/
dist/
.next/
target/
.anchor/
test-ledger/
keys/
.env
.env.*
!.env.example
*.log
.DS_Store
coverage/
# apps/web/public/idl and /deployments are committed on purpose: Vercel builds without `anchor build`, and the
# web app fetches /idl/peg_desk.json and /deployments/<cluster>.json at runtime. Refresh with `make idl web-deployments`.
apps/web/tsconfig.tsbuildinfo

# scripts/dev-postgres.ts data dir (embedded dev Postgres)
.pgdata/

# localnet deployments are per-ledger (anchor localnet resets); devnet/mainnet ones are committed
deployments/localnet.json
deployments/localnet.sql
.localnet-logs/
